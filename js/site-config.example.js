/**
 * Copie para `site-config.js` e ajuste os valores para o seu deploy.
 * O arquivo `site-config.js` pode ser versionado com a URL pública da API.
 */
window.__SITE_CONFIG__ = {
  commentsApiUrl: "https://SEU-PROJETO.vercel.app/api/send-comment",
  commentsClientSecret: "",
};
